# Example Internal Instances

These JSON files use the temporary internal schema from `autosolver.io`.
They are meant for quick inspection and CLI smoke tests before the official
contest format is released.

Run one with:

```powershell
python -m autosolver --input examples/tiny_manual.json
```
